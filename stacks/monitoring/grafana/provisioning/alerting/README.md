# Alerting Provisioning

This directory is intentionally empty of alerting definitions. It exists so
Grafana does not report a missing optional provisioning directory at startup.
HomeOps alert rules currently live in Prometheus.
